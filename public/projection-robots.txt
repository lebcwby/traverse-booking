# projection.booktraverse.com
#
# Vercel answers every attached domain with the whole app, so this host also
# serves all of booktraverse.com. Canonicals stop those duplicates being
# INDEXED, but not CRAWLED — that is how audit.booktraverse.com ended up with
# 56 crawled duplicates by 2026-08-24. Allow only the landing page this
# subdomain exists for.
#
# Served here by a host-scoped rewrite in next.config.ts. Doing it from a
# dynamic robots.ts instead 404'd robots.txt outright in production — see
# memory feedback_dynamic_robots_ts_404s_in_prod. Don't retry that.
User-Agent: *
Allow: /$
Disallow: /
